program SemanticFailure;

function xyz (data : array [3] of integer) : integer; 
begin
    var i, sum : integer;
	var j : boolean;
    i := 0; sum := 0;  
    while 1 + 1 do begin    
        read(j);  
		i := i + 1; 
    end; 
    return "jotain"; 
end; 

begin
    unknow := 123 + what;
	what := is / this;

	writeln(hmm);
	read(jotain);

	var1[1] := var2[hmm] - var4[index];

	true := false;

	var1[var2[1]] := var2[0];
end.